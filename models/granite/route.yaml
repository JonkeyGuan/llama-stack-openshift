apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: granite
  namespace: llama-serve
  annotations:
    haproxy.router.openshift.io/timeout: 2m
spec:
  to:
    kind: Service
    name: granite
  port:
    targetPort: 8000
